import axios from "axios";

const callDeepSeek = (prompt) => {
  // Trung và Nghĩa viết hàm gọi lên API của deepseek và gửi kèm theo nội dung
  // Dùng Axios để gọi API nha
};

const callGemini = (prompt) => {
  // Trung và Nghĩa viết hàm gọi lên API của gemini và gửi kèm theo nội dung
  // Dùng Axios để gọi API nha
};

export default {
  callDeepSeek,
  callDeepSeek,
};
